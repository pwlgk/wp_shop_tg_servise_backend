# Example Environment Variables for FastAPI Service
# Copy this file to .env and fill in your actual values.

# --- CORE SETTINGS ---
# A strong, randomly generated key for signing JWTs.
# Generate with: openssl rand -hex 32
SECRET_KEY=your_super_secret_jwt_key_here

# --- DATABASE (PostgreSQL) ---
DATABASE_USER=fastapi_user
DATABASE_PASSWORD=your_db_password
DATABASE_HOST=127.0.0.1
DATABASE_PORT=5433
DATABASE_NAME=fastapi_db

# --- CACHE (Redis) ---
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# --- TELEGRAM INTEGRATION ---
# Your main bot token from @BotFather
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
# Your bot's username (without @) for generating referral links
TELEGRAM_BOT_USERNAME=your_bot_username

# The public URL for setting the webhook. For local development, use an ngrok URL.
BASE_WEBHOOK_URL=https://your-public-api-domain.com
# A random secret string to verify requests from Telegram
TELEGRAM_WEBHOOK_SECRET=your_random_telegram_webhook_secret

# --- MINI APP ---
# The URL of your frontend Mini App
MINI_APP_URL=https://your-mini-app-domain.com

# --- WORDPRESS INTEGRATION ---
# The URL of your WordPress site
WP_URL=https://your-wordpress-domain.com
# Credentials from a WordPress Application Password for API authentication
WP_APP_USER=your_wp_app_user
WP_APP_PASSWORD=your_wp_app_password_without_spaces

# A secret string to verify webhooks from WooCommerce (for orders, products, etc.)
WP_WEBHOOK_SECRET=your_random_woocommerce_webhook_secret
# A secret string to verify the promo creation/update webhook
WP_PROMO_WEBHOOK_SECRET=your_random_promo_webhook_secret

# --- APPLICATION LOGIC ---
# The ID of the WordPress page used to store ACF settings
SHOP_SETTINGS_PAGE_ID=123

# Loyalty system settings in JSON format
LOYALTY_SETTINGS_JSON='{"bronze": {"cashback_percent": 3}, "silver": {"cashback_percent": 5}, "gold": {"cashback_percent": 7}}'
# How many days loyalty points are valid before expiring
POINTS_LIFETIME_DAYS=90

# --- ADMINISTRATION ---
# Comma-separated list of Telegram IDs for managers (receive order notifications, can reply to users)
ADMIN_TELEGRAM_IDS=12345678,87654321
# The ID of the private group/chat for operational notifications (new orders, etc.)
ADMIN_CHAT_ID=-1001234567890

# Comma-separated list of Telegram IDs for super-admins/developers (receive critical error alerts)
SUPER_ADMIN_IDS=12345678