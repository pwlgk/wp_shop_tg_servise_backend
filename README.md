# Backend Сервис для Telegram Mini App Магазина

Этот проект представляет собой высокопроизводительный сервисный слой (Backend for Frontend) на **FastAPI**, разработанный для обслуживания Telegram Mini App интернет-магазина. В качестве основного бэкенда и админ-панели для управления контентом используется **WordPress + WooCommerce** в headless-режиме.

## 🏛️ Архитектура

Система построена на принципах headless e-commerce, разделяя обязанности между тремя ключевыми компонентами для достижения максимальной гибкости и производительности.

```
┌──────────────────┐      ┌──────────────────┐      ┌─────────────────────┐
│ Telegram Mini App│      │   FastAPI Сервис │      │ WordPress/WooCommerce│
│ (Клиент)         ├──────► (Сердце)         ├──────► (Мозг)              │
└──────────────────┘      └──────────────────┘      └─────────────────────┘
       ▲                         ▲                               |
       │                         │                               │
       └─────────────────────────┴───────────────────────────────┘
                        Telegram Бот (Уведомления и CRM)
```

1.  **WordPress + WooCommerce (Мозг):**
    *   Выступает исключительно в роли админ-панели.
    *   Отвечает за хранение и управление товарами, категориями, заказами, пользователями и CMS-контентом (баннеры, страницы, акции) через плагин ACF.
    *   Предоставляет данные через стандартный WooCommerce REST API и несколько кастомных PHP-эндпоинтов для сложной логики.

2.  **FastAPI Сервис (Сердце и Нервная система):**
    *   Основной бэкенд, с которым взаимодействует клиентское приложение.
    *   Предоставляет версионированный REST API (`/api/v1`).
    *   Реализует всю кастомную бизнес-логику: система лояльности, реферальная программа, админ-панель.
    *   Агрессивно кеширует данные из WordPress в **Redis** для мгновенной отдачи.
    *   Управляет **Telegram-ботом** (`aiogram`) для отправки уведомлений и предоставления CRM-функционала администраторам.
    *   Хранит "быстрые" данные (корзина, избранное, уведомления) в собственной **PostgreSQL** базе данных.

3.  **Telegram Mini App + Бот (Лицо и Голос):**
    *   **Mini App:** Клиентское веб-приложение, которое получает все данные от FastAPI сервиса.
    *   **Бот:** Служит для аутентификации, отправки транзакционных уведомлений и как интерфейс для администраторов.

## ✨ Ключевой функционал

### Для Пользователя
-   **Полный цикл покупки:** От просмотра каталога до оформления заказа с поддержкой нескольких способов оплаты.
-   **Аутентификация:** Бесшовная регистрация и вход через Telegram.
-   **Персонализация:** Личный кабинет, история заказов, избранное.
-   **Система лояльности:** Многоуровневая система с кешбэком (бонусными баллами) за покупки и возможностью тратить их.
-   **Реферальная программа:** Генерация уникальных ссылок и получение бонусов за покупки друзей.
-   **Уведомления:** Получение в боте и Mini App уведомлений о статусе заказа, начислении/сгорании баллов и акциях.
-   **Обратная связь:** Возможность вести диалог с менеджером прямо в боте.

### Для Администратора
-   **CRM в Telegram:**
    -   Мгновенные уведомления о новых заказах в закрытую группу.
    -   Интерактивные кнопки для быстрой реакции (написать клиенту, ответить от бота, запросить контакт, перейти к заказу).
    -   Полноценный поиск пользователей (`/find_user`) и просмотр статистики (`/stats`).
    -   Пагинированный список пользователей с фильтрами (`/users`).
    -   Блокировка/разблокировка пользователей.
-   **Маркетинг:**
    -   Создание и запуск массовых рассылок (текст + фото) с таргетингом по уровню лояльности.
    -   Управление акциями (например, "Приветственный бонус") через команды в боте или админку WordPress.
-   **Мониторинг:**
    -   Автоматические уведомления о критических ошибках API в личные сообщения супер-администраторам.
-   **Защищенный API (`/api/v1/admin`):** Полный набор эндпоинтов для создания кастомного админского Mini App.

## 🛠️ Технологический стек

-   **Сервисный слой:** Python 3.11+, FastAPI
-   **База данных:** PostgreSQL
-   **Кеш:** Redis
-   **Взаимодействие с БД:** SQLAlchemy, Alembic
-   **Telegram Бот:** aiogram 3.x
-   **Фоновые задачи:** APScheduler
-   **Запуск в продакшене:** Docker, Gunicorn + Uvicorn
-   **CMS:** WordPress, WooCommerce, Advanced Custom Fields (ACF)

## 🚀 Настройка и запуск

### Предварительные требования
-   Python 3.11+
-   Docker и Docker Compose
-   Настроенный WordPress + WooCommerce на удаленном сервере.
-   Созданный Telegram-бот и полученный токен от @BotFather.

### 1. Настройка окружения
1.  Клонируйте репозиторий.
2.  Создайте файл `.env` на основе `.env.example` и заполните все необходимые переменные.
    ```bash
    cp .env.example .env
    nano .env
    ```
3.  Создайте и активируйте виртуальное окружение Python.
    ```bash
    python3 -m venv venv
    source venv/bin/activate
    ```
4.  Установите зависимости.
    ```bash
    pip install --no-cache-dir --upgrade pip
    pip install --no-cache-dir -r requirements.txt
    ```

### 2. Запуск инфраструктуры (для локальной разработки)
Эта команда поднимет контейнеры с PostgreSQL и Redis.
```bash
docker-compose up -d
```

### 3. Миграции базы данных
Перед первым запуском необходимо создать все таблицы в базе данных.
```bash
alembic upgrade head
```
*При изменении моделей в `app/models/` создавайте новую миграцию командой `alembic revision --autogenerate -m "Комментарий"`.*

### 4. Запуск приложения

#### Вариант А: Режим веб-хуков (основной)
Используется для продакшена и для разработки, если настроен `ngrok`.
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### Вариант Б: Режим поллинга (для отладки бота)
Этот режим не требует `ngrok` и позволяет быстро тестировать логику бота.
```bash
python run_polling.py
```

### API Документация
Интерактивная документация API (Swagger UI) доступна по адресу `http://localhost:8000/docs` после запуска Uvicorn.

---

## 🏗️ Развертывание (Deployment)

Для развертывания на продакшен-сервере используется `docker-compose.prod.yml`.

1.  **Сборка и запуск:**
    ```bash
    # Убедитесь, что на сервере есть актуальный .env файл
    docker compose -f docker-compose.prod.yml up --build -d
    ```
2.  **Применение миграций:**
    Миграции применяются автоматически при старте контейнера `api` с помощью скрипта `entrypoint.sh`. Для ручного применения:
    ```bash
    docker compose -f docker-compose.prod.yml exec api alembic upgrade head
    ```
3.  **Просмотр логов:**
    ```bash
    # Логи всех сервисов
    docker compose -f docker-compose.prod.yml logs -f
    
    # Логи только FastAPI сервиса
    docker compose -f docker-compose.prod.yml logs -f api
    ```

## 🗂️ Структура проекта

```
.
├── alembic/              # Миграции Alembic
├── app/                  # Основной код приложения
│   ├── bot/              # Логика Telegram-бота (хендлеры, сервисы, фильтры)
│   ├── clients/          # Клиенты для внешних API (WooCommerce)
│   ├── core/             # Конфигурация, логирование, ядро бота, Redis
│   ├── crud/             # Функции для прямого взаимодействия с БД
│   ├── db/               # Настройка сессии SQLAlchemy
│   ├── dependencies.py   # Зависимости FastAPI
│   ├── models/           # Модели SQLAlchemy
│   ├── routers/          # Роутеры FastAPI (API эндпоинты)
│   ├── schemas/          # Схемы Pydantic
│   ├── services/         # Бизнес-логика
│   └── utils/            # Вспомогательные утилиты
├── .env                  # Локальные переменные окружения (НЕ ДЛЯ GIT)
├── .env.example          # Шаблон для .env (ДЛЯ GIT)
├── Dockerfile            # Инструкция по сборке Docker-образа
├── docker-compose.yml    # Docker Compose для локальной разработки
├── docker-compose.prod.yml # Docker Compose для продакшена
├── entrypoint.sh         # Скрипт запуска для продакшен-контейнера
├── README.md             # Этот файл
├── requirements.txt      # Зависимости Python
└── run_polling.py        # Скрипт для запуска бота в режиме поллинга
```